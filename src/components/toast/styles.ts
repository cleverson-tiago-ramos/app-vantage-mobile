import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  toast: {
    position: "absolute",
    top: 40,
    left: 20,
    right: 20,
    padding: 14,
    borderRadius: 10,
    alignItems: "center",
    elevation: 5,
  },
  text: {
    color: "#fff",
    fontWeight: "600",
  },
});
